# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.

# Product knowledge lives under app/ after the monorepo layout move.
# Root docs/ holds methodology only (docs/superpowers/), not product docs.
[modules.bmm]
project_knowledge = "{project-root}/app"
