---
topic: MQTT additive sealed bridge (Teams network)
updated: 2026-09-11T11:10
---

- (decision) Additive MQTT+seal bridge only; local hive disk mail unchanged (AD-9)
- (decision) Inbound remote mail unseals then hive.send/deliver like webhook (AD-10)
- (decision) Same-machine delivery never uses MQTT (AD-11)
- (decision) Merge-friendly: new modules under app/src/main/network/; trivial diffs to hive/nudge (AD-12)
- (decision) Wire is sealed opaque boxes; MQTT QoS1 retain=false; broker never needs plaintext (AD-13)
- (constraint) Gated by plan===teams and canNetwork/canUse(network); community/pro/trial unchanged
- (constraint) Honor architecture spine Deferred/Rejected; no fat-nudge, no mailbox replacement, no invasive Materializer
- (capability) CAP-1: With Teams network, node can publish sealed outbound mail to a remote peer via MQTT
- (capability) CAP-2: With Teams network, node can subscribe, unseal, dedupe, and materialize inbound mail into local hive inbox
- (capability) CAP-3: Without network entitlement, no MQTT client traffic and local hive behavior identical to today
- (capability) CAP-4: Focused tests prove seal roundtrip and ciphertext-only broker observation for spike/MVP
- (assumption) Local Mosquitto (or equivalent) acceptable for MVP/spike; production hosted broker deferred
- (assumption) Outbound addressing MVP may be explicit API/org peer id rather than full outbox to= routing rewrite
- (question) Exact topic scheme and device-key distribution UX — lock in implementation epic or follow-on
- (event) Spec distilled from 2026-09-11-mqtt-relay spine+analysis+debate
- (event) SPEC.md + conventions + diagrams + epics M1-M5 derived and written
